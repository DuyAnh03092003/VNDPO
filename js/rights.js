window.VNDS_RIGHTS=(()=>{
  function init(){const grid=document.getElementById('rightsGrid');if(!grid)return;VNDS_DATA.RIGHTS.forEach(r=>{const card=document.createElement('article');card.className='card right-card';card.innerHTML=`<div class="card-icon">${r.icon}</div><h3>${r.name}</h3><p class="muted">${r.description}</p><div class="right-example"><strong>Ví dụ:</strong> ${r.example}</div><button class="btn btn-primary" data-right="${r.id}">Thực hiện quyền</button>`;grid.appendChild(card)});grid.addEventListener('click',e=>{const id=e.target.dataset.right;if(!id)return;const cfg=VNDS_STORAGE.getStorageItem(VNDS_STORAGE.KEYS.rights,VNDS_STORAGE.defaults.rights).find(x=>x.id===id);if(!cfg?.url){VNDS_MODAL.open({title:'Chưa cấu hình đường dẫn',content:'<p>Đường dẫn thực hiện quyền này chưa được cấu hình. Vui lòng sử dụng nút Cài đặt Demo để bổ sung đường dẫn.</p>',size:'sm'});return}if(!VNDS_UTILS.isValidUrl(cfg.url)){VNDS_TOAST.show('URL đã cấu hình không hợp lệ.','error');return}if(/^https?:\/\//.test(cfg.url))window.open(cfg.url,'_blank','noopener,noreferrer');else location.href=cfg.url})}
  return {init};
})();
