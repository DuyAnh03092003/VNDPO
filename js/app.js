document.addEventListener('DOMContentLoaded',()=>{
  VNDS_LAYOUT.init();VNDS_SETTINGS.init();VNDS_COOKIE.applySaved();VNDS_PRODUCTS.renderHome();VNDS_REGISTRATION.init();VNDS_RIGHTS.init();VNDS_RIGHT_REQUEST.init();
});
