window.VNDS_MODAL=(()=>{
  let lastFocus=null;
  function close(backdrop){if(!backdrop)return;backdrop.remove();document.body.style.overflow='';lastFocus?.focus?.()}
  function open({title,content,footer='',size='lg',onClose}){lastFocus=document.activeElement;const backdrop=document.createElement('div');backdrop.className='modal-backdrop';const modal=document.createElement('section');modal.className=`modal ${size==='sm'?'modal-sm':''}`;modal.setAttribute('role','dialog');modal.setAttribute('aria-modal','true');const header=document.createElement('div');header.className='modal-header';const h=document.createElement('h2');h.textContent=title;const x=document.createElement('button');x.className='icon-btn';x.setAttribute('aria-label','Đóng');x.textContent='✕';header.append(h,x);const body=document.createElement('div');body.className='modal-body';if(typeof content==='string')body.innerHTML=content;else body.append(content);modal.append(header,body);if(footer){const foot=document.createElement('div');foot.className='modal-footer';if(typeof footer==='string')foot.innerHTML=footer;else foot.append(footer);modal.append(foot)}backdrop.append(modal);document.body.append(backdrop);document.body.style.overflow='hidden';const doClose=()=>{onClose?.();close(backdrop)};x.onclick=doClose;backdrop.addEventListener('click',e=>{if(e.target===backdrop)doClose()});backdrop.addEventListener('keydown',e=>{if(e.key==='Escape')doClose();if(e.key==='Tab'){const f=[...modal.querySelectorAll('button,a,input,textarea,select,[tabindex]:not([tabindex="-1"])')].filter(x=>!x.disabled);if(!f.length)return;const first=f[0],last=f[f.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}});setTimeout(()=>modal.querySelector('button,input,textarea,select,a')?.focus(),0);return {backdrop,modal,body,close:doClose}}
  function confirm({title='Xác nhận',message,confirmText='Xác nhận',danger=false}){return new Promise(resolve=>{const content=document.createElement('div');content.innerHTML=`<p>${message}</p>`;const footer=document.createElement('div');footer.style.display='contents';const cancel=document.createElement('button');cancel.className='btn btn-secondary';cancel.textContent='Hủy';const ok=document.createElement('button');ok.className=`btn ${danger?'btn-danger':'btn-primary'}`;ok.textContent=confirmText;const m=open({title,content,footer});footer.append(cancel,ok);cancel.onclick=()=>{m.close();resolve(false)};ok.onclick=()=>{m.close();resolve(true)}})}
  return {open,confirm};
})();
